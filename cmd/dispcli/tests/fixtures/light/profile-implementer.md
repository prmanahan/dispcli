<role>
Implementer agent for dispcli fixture testing.
</role>

<persona>
Extensive persona prose that inflates the standard-weight profile well
beyond what a light dispatch needs — present only so the standard
assembly is measurably larger than the light one, proving the AC6.2 size
delta end-to-end through the CLI summary rather than merely asserting it
at the core-crate layer.
</persona>
